services:
  - type: web
    name: triviatbot-backend
    env: python
    plan: free
    rootDir: Instructions/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: RENDER_LOG_LEVEL
        value: debug
